% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truncatedscore.R
\name{estimate_truncatedscore}
\alias{estimate_truncatedscore}
\title{Estimation of mean clinical outcome truncated by event process}
\usage{
estimate_truncatedscore(
  data,
  mod.y,
  mod.r,
  mod.a,
  mod.event,
  time,
  cause = 1,
  cens.code = 0,
  naive = FALSE,
  ...
)
}
\arguments{
\item{data}{data.frame}

\item{mod.y}{model for clinical outcome given T>time}

\item{mod.r}{model for missing data mechanism for clinical outcome at T=time}

\item{mod.a}{treatment model (in RCT should just be 'a ~ 1')}

\item{mod.event}{Model for time-to-event process ('Event(time,status) ~ x')}

\item{time}{landmark time}

\item{cause}{primary event (in the 'status' variable of the 'Event'
statement)}

\item{cens.code}{censoring code (0 default)}

\item{naive}{if TRUE the unadjusted estimates ignoring baseline covariates
is returned as the attribute 'naive'}

\item{...}{additional arguments passed to \link[mets:binregATE]{mets::binregATE}}
}
\value{
estimate object
}
\description{
Let \eqn{Y} denote the clinical outcome, \eqn{A} the binary treatment
variable, \eqn{X} baseline covariates, \eqn{T} the failure time,
and \eqn{epsilon=1,2} the cause of failure.
The following are our two target parameters
\deqn{E(Y|T>t, A=1)- E(Y|T>t, A=0)}
\deqn{P(T<t,\epsilon=1|A=1)- P(T<t,\epsilon=1|A=0)}
}
\examples{
\dontrun{
mod1 <- predictor_glm(y ~ a * (x1 + x2))
mod2 <- predictor_glm(r ~ a * (x1 + x2), family = binomial)
a <- estimate_truncatedscore(
  data = dat,
  mod.y = mod1,
  mod.r = mod2,
  mod.a = a ~ 1,
  mod.event = mets::Event(time, status) ~ a * (x1+x2),
  time = 2
)

s <- summary(a, noninf.t = -0.1)
print(s)
parameter(s)
}
}
\author{
Klaus Kähler Holst
}
