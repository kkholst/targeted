% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/condlogit.R
\name{condlogit}
\alias{condlogit}
\title{Conditional logit model}
\usage{
condlogit(
  choice = NULL,
  x = ~1,
  z1 = ~0,
  z2 = ~0,
  weights = NULL,
  alt = NULL,
  id = NULL,
  data = parent.frame(),
  reference = 1,
  control = list(),
  method = "nlminb",
  lambda2 = 0,
  debug = FALSE,
  ...
)
}
\arguments{
\item{choice}{Vector of choices (1,0) for each alternative or string with column-name in the data. Alternatively, a formula (see details below).}

\item{x}{Individual specific covariates. Matrix or formula.}

\item{z1}{Alternative specific covariates (constant effect on utility within each alternative). Matrix or formula.}

\item{z2}{Alternative specific covariates (effect on utility varies for each alternative). Matrix or formula.}

\item{weights}{frquency weights (vector or column-name)}

\item{alt}{Vector of alternatives or character string with the column-name}

\item{id}{Vector of individual identifiers or character string with the column-name}

\item{data}{data.frame}

\item{reference}{Base alternative (reference)}

\item{control}{Optimization arguments}

\item{method}{Type of optimization}

\item{lambda2}{l2 regularization}

\item{debug}{debug}

\item{...}{Additional arguments to lower level functions}
}
\value{
Object of type \code{condlogit}.
}
\description{
Conditional (multinomial) logit model for discrete choice experiments (McFadden 1974)
}
\details{
'choice' can be specified as formula with the following syntax:
`choice ~ alternatives | id` thus specifying both 'choice', 'alt', and 'id' which are automatically extracted from the data.frame.
The data.frame is in this case assumed to be on a long format where each row represents the choice and features for each alternative.
The set of alternatives may vary between individuals.  Each type of covariate should then also be defined by separate formulas, e.g.,
`x=1+x1+x2`, `z1=~z1`, `z2=~z2` (some potentially omitted).

It is also possible to omit the "bar" notation, e.g., using the syntax
`chosen_alternative ~ 1+x1+x2 ...`. In this case the data is assumed to be in short form where there are no alternative specific covariates (z1=z2=NULL),
and the data.frame only contains the rows where `choice==1` (similar to for example mets::mlogit or nnet::multinom).
}
\examples{
data(iris)
g <- condlogit(Species ~ Sepal.Width*Petal.Length, data=iris)
pr <- predict(g, newdata=iris, wide=TRUE)
cl <- colnames(pr)[apply(pr, 1, which.max)]
table(iris$Species, cl)
}
\author{
Klaus K. Holst
}
