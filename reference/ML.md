# ML model

Wrapper for ml_model

## Usage

``` r
ML(formula, model = "glm", ...)
```

## Arguments

- formula:

  formula

- model:

  model (sl, rf, pf, glm, ...)

- ...:

  additional arguments to model object
