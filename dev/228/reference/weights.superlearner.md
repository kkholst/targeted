# Extract ensemble weights

Extract ensemble weights

## Usage

``` r
# S3 method for class 'superlearner'
weights(object, ...)
```

## Arguments

- object:

  (superlearner) Fitted model.

- ...:

  Not used.
