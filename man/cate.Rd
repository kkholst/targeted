% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cate.R
\name{cate}
\alias{cate}
\title{Conditional Average Treatment Effect estimation}
\usage{
cate(
  response.model,
  treatment.model,
  cate.model = ~1,
  calibration.model = NULL,
  missing.model = NULL,
  data,
  contrast,
  nfolds = 1,
  rep = 1,
  id = NULL,
  silent = FALSE,
  stratify = FALSE,
  mc.cores = NULL,
  var.type = "IC",
  second.order = TRUE,
  response_model = deprecated,
  cate_model = deprecated,
  propensity_model = deprecated,
  propensity.model = deprecated,
  treatment = deprecated,
  ...
)
}
\arguments{
\item{response.model}{formula or learner object (formula => learner_glm)}

\item{treatment.model}{formula or learner object (formula => learner_glm)}

\item{cate.model}{formula specifying regression design for conditional
average treatment effects}

\item{calibration.model}{linear calibration model. Specify covariates in
addition to predicted potential outcomes to include in the calibration.}

\item{missing.model}{formula or learner object; default \code{NULL}. Model for the
missingness mechanism \eqn{P(R=1 \mid X, A)}. Required when the outcome in
\code{response.model} contains NAs. If the formula LHS is omitted, the
observation indicator is used automatically. When \code{stratify = TRUE} the
missing model is fit separately per treatment arm. When supplied, the AIPW
score is inverse-probability-of-observation weighted and (if \code{second.order = TRUE} and \code{stratify = FALSE}) an additional second-order term is added to
the influence function.}

\item{data}{data.frame}

\item{contrast}{treatment contrast (default 1 vs 0)}

\item{nfolds}{number of folds (positive integer), or a pre-specified list of
fold indices where each element is an integer vector of observation indices
forming a partition of \code{1:nrow(data)}.}

\item{rep}{number of replications of cross-fitting procedure by averaging
estimates and influence functions from each replication}

\item{id}{(integer or character) optional subject id vector of length
\code{nrow(data)}. The \code{id} can also be specified as part of the \code{cate.model}
argument with a formula syntax: \code{~ 1 + cluster(id)}.}

\item{silent}{suppress all messages and progressbars}

\item{stratify}{if TRUE the response.model will be stratified by treatment}

\item{mc.cores}{(optional) number of cores. parallel::mcmapply used instead
of future}

\item{var.type}{when equal to "IC" the asymptotic variance is derived from
the influence function. Otherwise, based on expressions in Bannick et al.
(2025) valid under different covariate-adaptive randomization schemes (only
available for ATE and when \code{calibration.model} is also specified)}

\item{second.order}{add seconder order term to IF to handle misspecification
of outcome models}

\item{response_model}{Deprecated. Use response.model instead.}

\item{cate_model}{Deprecated. Use cate.model instead.}

\item{propensity_model}{Deprecated. Use treatment.model instead.}

\item{propensity.model}{Deprecated. Use treatment.model instead.}

\item{treatment}{Deprecated. Use cate.model instead.}

\item{...}{additional arguments to future.apply::future_mapply}
}
\value{
cate.targeted object
}
\description{
Conditional Average Treatment Effect estimation with cross-fitting.
}
\details{
We have observed data \eqn{(Y,A,W)} where \eqn{Y} is the response variable,
\eqn{A} the binary treatment, and \eqn{W} covariates. We further let \eqn{V}
be a subset of the covariates. Define the conditional potential mean outcome
\deqn{\psi_{a}(P)(V) = E_{P}[E_{P}(Y\mid A=a, W)|V]} and let \eqn{m(V;
\beta)} denote a parametric working model, then the target parameter is the
mean-squared error \deqn{\beta(P) = \operatorname{argmin}_{\beta}
E_{P}[\{\Psi_{1}(P)(V)-\Psi_{0}(P)(V)\} - m(V; \beta)]^{2}}

Missing data is handled under a Missing At Random assumption (MAR). Let
\eqn{R} denote the indicator for data not being missing, \eqn{R\perp Y|W,A}.
The nuisance models are \eqn{Q(w,a) = E(Y|W=w, A=a)}, \eqn{g_a(w) =
P(A=a|W=w)}, and \eqn{\rho(w, a) = P(R=1|W=w, A=a)}. For the expected
potential outcome \eqn{E[Y(a)]}, the AIPW estimator then takes the form
\deqn{\frac{1}{n}\sum_{i=1}^n R_i I(A_i=a) / \{g_a(W_i) \rho(W_i, a)\} (Y_i -
Q(W_i,a)) + Q(W_i, a)}.
}
\examples{
sim1 <- function(n=1000, ...) {
  w1 <- rnorm(n)
  w2 <- rnorm(n)
  a <- rbinom(n, 1, plogis(-1 + w1))
  y <- cos(w1) + w2*a + 0.2*w2^2 + a + rnorm(n)
  data.frame(y, a, w1, w2)
}

d <- sim1(5000)
## ATE
cate(cate.model=~1,
     response.model=y~a*(w1+w2),
     treatment.model=a~w1+w2,
     data=d)
## CATE
cate(cate.model=~1+w2,
     response.model=y~a*(w1+w2),
     treatment.model=a~w1+w2,
     data=d)

\dontrun{ ## superlearner example
mod1 <- list(
   glm = learner_glm(y~w1+w2),
   gam = learner_gam(y~s(w1) + s(w2))
)
s1 <- learner_sl(mod1, nfolds=5)
cate(cate.model=~1,
     response.model=s1,
     treatment.model=learner_glm(a~w1+w2, family=binomial),
     data=d,
     stratify=TRUE)
}

}
\references{
Mark J. van der Laan (2006) Statistical Inference for Variable
Importance, The International Journal of Biostatistics.

Bannick, Shao & Liu et al. (2025) A General Form of Covariate Adjustment in
Clinical Trials under Covariate-Adaptive Randomization, Biometrika.
}
\author{
Klaus Kähler Holst, Andreas Nordland
}
