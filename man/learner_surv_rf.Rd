% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/learner_surv_rf.R
\name{learner_surv_rf}
\alias{learner_surv_rf}
\title{Construct a learner}
\usage{
learner_surv_rf(
  formula,
  info = "survival forest (ranger)",
  num.threads = 1L,
  learner.args = NULL,
  ...
)
}
\arguments{
\item{formula}{(formula) Formula specifying response and design matrix.}

\item{info}{(character) Optional information to describe the instantiated
\link{learner} object.}

\item{num.threads}{Number of threads. Use 0 for all available cores. Default is 2 if not set by options/environment variables (see below).}

\item{learner.args}{(list) Additional arguments to
\link[=learner]{learner$new()}.}

\item{...}{Further arguments passed to or from other methods (currently ignored).}
}
\value{
\link{learner} object.
}
\description{
Constructs a \link{learner} class object for random survival forests
}
\examples{
data(sTRACE, package="mets")
mod <- learner_surv_rf(Surv(time, status>0) ~ sex + strata(age))
mod$estimate(sTRACE)
mod$predict(head(sTRACE), time=5) # P(T>t|X)
}
\author{
Klaus Kähler Holst
}
