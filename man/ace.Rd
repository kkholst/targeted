% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ace.R
\name{ace}
\alias{ace}
\alias{aipw}
\title{AIPW estimator for Average Causal Effect}
\usage{
ace(
  formula,
  exposure = NULL,
  data,
  weights,
  binary = TRUE,
  all,
  missing = FALSE,
  labels = NULL,
  ...
)
}
\arguments{
\item{formula}{Formula (see details below)}

\item{exposure}{optional exposure formula}

\item{data}{data.frame}

\item{weights}{optional frequency weights}

\item{binary}{Binary response (default TRUE)}

\item{all}{If TRUE all standard errors are calculated (default TRUE when exposure only has two levels)}

\item{missing}{If TRUE a missing data (AIPW) estimator is returned}

\item{labels}{Optional treatment labels}

\item{...}{Additional arguments to lower level functions}
}
\description{
Augmented Inverse Probability Weighting estimator for the Average Causal Effect.
}
\examples{
m <- lvm(y ~ a+x, a~x)
distribution(m,~ a+y) <- binomial.lvm()
d <- sim(m,1e3,seed=1)
a <- ace(y ~ a|x|x, data=d)
summary(a)

# Multiple treatments
m <- lvm(y ~ a+x, a~x)
distribution(m,~ y) <- binomial.lvm()
m <- ordinal(m, K=4, ~a)
transform(m, ~a) <- factor
d <- sim(m,1e5)
(a <- ace(y~a|a*x|x, data=d))

# Comparison with randomized experiment
m0 <- cancel(m, a~x)
d0 <- sim(m0,2e5)
lm(y~a-1,d0)

# Choosing a different contrast for the association measures
summary(a, contrast=c(2,4))
}
\author{
Klaus K. Holst
}
