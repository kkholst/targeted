## -*- Makefile -*-

# CXX_STD = CXX14

PKGROOT=target
# SOURCES = $(wildcard ./*.cpp) $(wildcard $(PKGROOT)/src/*.cpp)
OBJECTS =  target/src/glm.dll \
	target/src/nb.dll \
	target/src/nondom.dll \
	target/src/odesolver.dll \
	target/src/pava.dll \
	target/src/target.dll \
	target/src/utils.dll


PKG_CXXFLAGS = $(SHLIB_OPENMP_CXXFLAGS) -DARMA_R -I. -I$(PKGROOT)/include -I../inst/include
PKG_LIBS = $(SHLIB_OPENMP_CXXFLAGS) $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
