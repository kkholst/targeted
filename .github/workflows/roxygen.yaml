# Check if documentation is up to date using roxygen2
on:
  pull_request:
    branches: [main, dev]

name: check-roxygen

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-roxygen:
    runs-on: ${{ matrix.config.os }}
    timeout-minutes: 30
    name: ${{ matrix.config.os }} (${{ matrix.config.r }})
    strategy:
      matrix:
        config:
          - {os: ubuntu-latest,   r: 'release'}

    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      R_KEEP_PKG_SOURCE: yes

    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: ${{ matrix.config.r }}
          http-user-agent: ${{ matrix.config.http-user-agent }}
          use-public-rspm: true

      - uses: r-lib/actions/setup-r-dependencies@v2
        with:
          extra-packages: any::devtools
          dependencies: '"hard"'

      - name: Regenerate roxygen docs and check for changes
        run: |
          make roxygen
          # -N (intent-to-add) makes new, untracked man/ files visible to
          # `git diff` so newly generated .Rd files also fail the check.
          git add -A -N
          git diff --exit-code
